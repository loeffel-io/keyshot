load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_test")

rust_binary(
    name = "main",
    srcs = [
        "main.rs",
        "selection.rs",
    ],
)

rust_test(
    name = "main_test",
    crate = ":main",
)
